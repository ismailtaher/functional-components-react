import React from "react";

const Header = () => {
  return (
    <header
      style={{
        backgroundColor: "mediumblue",
        color: "#FFF",
      }}>
      <h1>Grocceries List</h1>
    </header>
  );
};

export default Header;
